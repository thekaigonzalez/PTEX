import std.stdio;
import std.file;
import std.array
void main() {
// any includes in here, will be in the main function. 
// nothing is going to be added outside of this. 
writeln("hello." );
writeln("World." );
writeln("This" );
writeln("is" );
writeln("Equal" );
writeln(1 + 1 );
}
